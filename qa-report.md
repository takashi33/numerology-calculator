# QAレポート：数秘電卓（試作版）

## 実施したチェック
- `node --check script.js` による構文エラーチェック → OK
- 計算ロジック（ライフパスナンバー・デスティニーナンバー・チャレンジナンバー・ソウルナンバー・パーソナリティナンバー）を、Node.js上で手計算例と突き合わせて検証
  - 例：1990年7月15日 → ライフパスナンバー 5、チャレンジナンバー [1, 5, 4, 6]（手計算と一致）
  - 例：1978年12月14日 → ライフパスナンバー 33（マスターナンバー。桁をすべて合計する方式に変更後、正しく33になることを確認）
  - 例：1990年7月15日 → ピナクルナンバー [4, 7, 11, 8]（第3ピナクルがマスターナンバー11になることを確認）、2026年のパーソナルイヤーナンバー 5
  - 例：ライフパスナンバー5 → 年齢範囲 [0〜31歳, 32〜40歳, 41〜49歳, 50歳〜]（36−5=31を起点にした標準的な区分と一致することを確認）
  - 不具合修正：ライフパスナンバーがマスターナンバー（例：33）の場合、そのまま「36−33」を計算すると第1期が「0〜3歳」という不自然な範囲になるバグがあった。年齢範囲の計算時だけマスターナンバーも1桁まで還元するよう修正し、LP=33→「0〜30歳」、LP=11→「0〜34歳」と自然な範囲になることを確認
  - 例：名前「Eve」→ 合計14 → カルミックデット・ナンバーとして正しく検出されることを確認
  - 例：名前「Taro Yamada」→ デスティニーナンバー 9、ソウルナンバー 1、パーソナリティナンバー 8（ソウル+パーソナリティの合計値がデスティニー計算時の合計値36と一致することも確認）

## 追記：チャレンジナンバー（基本の3数字）の計算方式を変更
オーナーからのご指摘を受け、「基本の3数字」のチャレンジナンバーを、欧米式（月日をそれぞれ1桁に還元してから差を取る）から、日本の数秘術でよく使われる「未来数」式（月日の桁を全てバラして合計する）に変更した（`app/script.js`・`webapp/index.html`の両方に反映）。
- 例：1978年12月14日 → 月「12」の桁1+2、日「14」の桁1+4 → 合計8 → チャレンジナンバー8（変更前は差の計算で2だった）
- 「チャレンジナンバー 詳細（人生の4つの時期）」の第1〜第4チャレンジは欧米式の差の計算のまま変更していない（オーナーの意向により、基本の3数字のみ変更する範囲とした）。そのため基本の3数字のチャレンジナンバーと詳細セクションの第1チャレンジが異なる値になる場合がある（spec.mdに明記済み）
- Node.js上で計算式を検証：1978-12-14→8、1990-07-15→4、1990-09-29→2（未来数の理論上の最大値20のケース）、1990-09-02→11（マスターナンバーが出るケース）を確認
- CHALLENGE_MEANINGS（一言解説の対応表）に、新たに出現しうる「9」と「11（マスターナンバー）」の解説文を追加

## 追記：新しいタブで「扉を開く」ボタンが反応しない不具合を修正
オーナーから「新しいタブで開くと扉を開くボタンが反応しない」との報告を受け調査。`sessionStorage`への読み込み処理が、ボタンのクリック処理を登録するコードより前に無防備に実行されており、新しいタブ（特にArtifactの共有リンクを直接開いた場合）ではブラウザのプライバシー制限で`sessionStorage`へのアクセス自体が例外を投げることがあり、その場合スクリプト全体が停止してボタンのクリック処理が登録されない状態になっていた。`sessionStorage`の読み込み・書き込みの両方をtry/catchで保護し、アクセスに失敗してもボタンの反応やページの動作自体は続行するよう修正（`webapp/index.html`）。
また、オーナーの要望により画面上の「合言葉」という表記を「コード入力」（または文脈に応じて「コード」）に変更した（`webapp/index.html`・`webapp/invite-card.html`）。

## 追記：招待カードのQRコードを読み取るとClaudeのチャット画面に飛ぶ不具合を修正
オーナーから「QRコードを読み取ったらClaudeのチャット画面に飛んだ」との報告を受け調査。招待カード（`webapp/invite-card.html`）に埋め込まれていたQRコード画像が、意図した数秘電卓のArtifact URL（`https://claude.ai/code/artifact/c44b2f9d-...`）とは別のURL（作成時のチャット画面等）を指すものになっていたと考えられる。
古い画像がどのURLを指していたか手元では確認できなかった（QRデコードツールがローカル環境になかったため）ため、原因の特定よりも修正を優先し、`qrcode`（npmパッケージ、既にローカルにインストール済み）を使って、正しい数秘電卓のArtifact URLを直接エンコードしたQRコード画像を再生成し、招待カードの画像を丸ごと差し替えた。生成後、画像を目視で確認し、3つの位置検出パターンを含む正常な見た目のQRコードであることを確認済み。

## 追記：Claude Artifactでの公開をやめ、GitHub Pagesに移行
招待カードのQRコード修正後も、スマホでQRコードを読み取るとClaudeアカウントのログイン画面（チャット画面）に飛んでしまう不具合が解消しなかった。共有設定（「Anyone with the link」）をオンにし、ログイン済みの状態で試しても再現したため、Claude Codeの「Artifact」機能自体が、閲覧側にも何らかの条件（Claudeアカウントでの認証等）を要求する仕組みになっている可能性が高いと判断。オーナーと相談の上、Artifactでの公開をやめ、無料の外部Webホスティングサービス（GitHub Pages）に完全移行することにした。
- オーナー自身のGitHubアカウント（`takashi33`）で、`numerology-calculator`という名前のリポジトリを作成し、`webapp/index.html`・`webapp/invite-card.html`をアップロードしてもらう形で移行（アカウント作成・ログインを伴う操作はオーナー本人に実施してもらった。AI社員側はログイン情報を扱えないため）
- 移行作業中、（1）リポジトリ名を`-`という記号のみにしてしまい正しく公開されない、（2）アップロード時に別プロジェクト（ナンバーマージ）のファイルを誤って選んでしまいGitHub Pages自体は正常なのに中身が別ゲームになってしまう、という2つのつまずきがあった。原因切り分けの際は、リポジトリ内のファイルの中身を直接（`raw.githubusercontent.com`経由で）確認するのが有効だった
- 最終的な公開先：
  - 数秘電卓本体：`https://takashi33.github.io/numerology-calculator/`
  - 招待カード：`https://takashi33.github.io/numerology-calculator/invite-card.html`
- 合言葉（コード）方式のゲート機能自体は変更なし、そのまま引き継いだ

## 追記：アプリ内に「招待QRコードを表示（管理者用）」ボタンを追加
オーナーの要望により、`webapp/index.html`のフッター部分に、招待用のQRコードとコードをその場でポップアップ表示できるボタンを追加した。合言葉を知っている人なら誰でも押せる簡易的なボタンであり、本格的な管理者専用の認証機能ではない旨をオーナーに明示した上で実装。

## 追記：管理者用コードと招待者用コードを分けて発行
上記のボタンが「コードを知っている人なら誰でも押せる」状態だった点について、オーナーから「管理者と招待者の識別」を求められたため、コードを2種類に分けた。
- 招待者用コード：`19810619`（従来通り。入力すると数秘電卓は使えるが、招待QRコードのボタンは表示されない）
- 管理者用コード：`19781214`（新規。入力すると数秘電卓に加えて「招待QRコードを表示（管理者用）」ボタンも表示される）

どちらのコードで入ったかを`sessionStorage`に記録し（キー名を`numerology-gate-unlocked`から`numerology-gate-role`に変更、値は`"admin"`か`"guest"`）、ページ再読み込み時もどちらの権限だったかを覚えておくようにした。招待QR内に表示されるコードは、招待者用コード（19810619）のまま。

## 追記：ライフレッスンナンバー・インテンシティナンバーを追加
オーナーが外部の数秘術サイト（マンガ数秘らぼ）で自身の結果（生年月日1978-12-14、名前Sekiguchi Takashi）を確認したところ、本アプリの計算結果（ライフパス33・デスティニー8・ソウル1・パーソナリティ7・バースデー5・成熟数5）がすべて一致することを確認。加えて同サイトにあった「LL（ライフレッスンナンバー）」「IT（インテンシティナンバー）」の2つを本アプリにも追加した。
- **ライフレッスンナンバー**：名前の各アルファベットを数字に変換したときに1〜9のうち登場しない数字（複数・0個の場合あり）。Sekiguchi Takashiの例で[4, 6]と、外部サイトの結果と一致することをNode.js上で確認
- **インテンシティナンバー**：名前の各アルファベットを数字に変換したときに最も多く登場する数字（同数タイの場合は複数表示）。同じ例で[1]と、外部サイトの結果と一致することを確認
- 表示欄が「1つの数字」ではなく「0〜複数個の数字」になりうるため、専用の`createMultiNumberCard`関数を新設し、「名前から分かる数字」グループの末尾に追加（`app/script.js`・`webapp/index.html`の両方に反映）
- なお、同サイトの「カルミックレッスンナンバー」という呼び方は、計算方法としては今回の「ライフレッスンナンバー」と同じ概念（英語圏と日本語圏での呼び方の違い）。別途アプリに実装済みの「カルミックデット・ナンバー（13・14・16・19）」とは異なる概念なので注意
- GitHub Pagesへの反映後、オーナー自身のスマホで、ライフレッスンナンバー・インテンシティナンバーが正しく表示されることを確認済み

## 追記：パーソナルマンスナンバー・パーソナルデイナンバーを追加
オーナーの要望により、「今年の数字」セクションを「今年・今月・今日の数字」に拡張し、パーソナルマンスナンバー（今月の数字）・パーソナルデイナンバー（今日の数字）を追加した。
- パーソナルマンスナンバー＝パーソナルイヤーナンバー＋計算月（還元せず1〜12のまま）を合計し還元
- パーソナルデイナンバー＝パーソナルマンスナンバー＋計算日（還元せず1〜31のまま）を合計し還元
- 海外の数秘術解説サイトの計算例（パーソナルイヤー5・10月→パーソナルマンス6、パーソナルマンス5・12日→パーソナルデイ8）と、Node.js上での検証で一致することを確認
- `app/script.js`・`webapp/index.html`の両方、および`app/index.html`の見出しテキストに反映

## 追記：スマホのホーム画面にアイコンとして追加できるようにした
オーナーの要望により、スマホのブラウザの「ホーム画面に追加」機能で、数秘電卓がアプリのようなアイコン・タップ起動になるよう対応した。
- アプリのゲート画面にある「同心円の紋章」デザインを模したアイコン画像を、Pythonの`Pillow`ライブラリで新規作成（512×512・192×192・180×180の3サイズ）
- `manifest.json`相当の内容（アプリ名・アイコン・`display: standalone`など）を、外部ファイルを増やさずBase64データURIとして`webapp/index.html`の`<link rel="manifest">`に埋め込み
- iOS向けに`apple-touch-icon`・`apple-mobile-web-app-capable`等のmetaタグも追加
- サービスワーカーは実装していないため、Androidの自動インストールバナーは出ない可能性がある。その場合もブラウザメニューからの手動「ホーム画面に追加」は可能
- 上記のアイコンはオーナーから「ダサい」との指摘を受け、ChatGPTで生成した画像（三日月・星・1〜9の数字を配した紋章デザイン、`icon-source.png`として保存）に差し替え。512×512・192×192・180×180にリサイズし、manifest・apple-touch-iconの両方に反映済み

## 追記：各カードをタップすると「〇〇ナンバーとは？」の説明が開くようにした
オーナーの要望により、各結果カードに常時表示している「1〜9の意味の一言解説」とは別に、その数字の**種類そのもの**（計算方法・何を表すとされているか）を説明するポップアップを追加した。
- 全14種類のカード（ライフパス・バースデー・チャレンジ・パーソナルイヤー/マンス/デイ・チャレンジ詳細・ピナクル・デスティニー・ソウル・パーソナリティ・ライフレッスン・インテンシティ・成熟数）それぞれに説明文（`GLOSSARY`）を用意
- カードをタップ（クリック）すると、画面中央にポップアップで「〇〇ナンバーとは？」というタイトルと説明文を表示。カードには小さく「ⓘ」マークを表示し、タップできることが分かるようにした
- `app/script.js`・`app/index.html`・`app/style.css`・`webapp/index.html`すべてに反映

## 追記：生年月日の入力を「年・月・日」3つの数字ボックスに変更
オーナーの要望により、生年月日の入力欄をブラウザ標準の`<input type="date">`から、年・月・日それぞれ独立した数字専用の入力ボックス3つに変更した。
- 年のボックスに4桁入力すると自動的に月のボックスへ、月のボックスに2桁入力すると自動的に日のボックスへ、それぞれフォーカスが移動する（「19781214」と数字だけ連続して入力できる）
- 空のボックスでBackspaceキーを押すと、1つ前のボックスに戻る
- 年のボックスに「19781214」のような8桁をまとめて貼り付けた場合も、年・月・日に自動で振り分ける
- ブラウザ標準の日付入力が持っていた「実在する日付かどうか」の検証機能が失われるため、`Date`オブジェクトを使って手動で実在日チェック（2月30日等の不正な日付を弾く）を追加
- `app/script.js`・`app/index.html`・`app/style.css`・`webapp/index.html`すべてに反映

## 追記：「チャレンジナンバー詳細」「ピナクルナンバー」の見出しを変更
オーナーから「チャレンジナンバー詳細」という名前だと、基本の3数字のチャレンジナンバーの延長線上のように誤解されるとの指摘があり（実際には計算方法も流派も別物）、見出しを変更した。
- 「チャレンジナンバー 詳細（人生の4つの時期の課題）」→「ステージ別の課題」
- 「ピナクルナンバー（人生の4つの時期の機会）」→「ステージ別のチャンス」（対になる見出しとして統一感を持たせるため、こちらも変更）
- その後、オーナーの追加要望により、個々のカードのラベルも「第1チャレンジ」→「第1期の課題」、「第1ピナクル」→「第1期のチャンス」に変更（計算ロジックは変更なし）
- `spec.md`内の「第1チャレンジ」「第1ピナクル」等の表記もあわせて更新
- `webapp/index.html`・`app/index.html`・`app/script.js`・`spec.md`に反映

## 追記：オフライン対応（サービスワーカー）を追加
電波が悪い場所や圏外でもアプリを開けるよう、サービスワーカーを追加した。
- `webapp/sw.js`を新規作成。ページ本体（`index.html`）をキャッシュに保存し、次回以降はネット接続がなくても保存済みの内容を表示する（アプリ自体が外部CSS/JS/画像を一切読み込まない自己完結型なので、キャッシュ対象はページ本体のみで十分）
- `webapp/index.html`に、ページ読み込み時にこのサービスワーカーを登録する処理を追加
- サービスワーカーはブラウザの仕様上、`file://`で開くローカル版（`app/`フォルダ）では動作しない（無害なノーオペレーションになる）。GitHub Pagesなどhttps経由で開いた場合のみ有効
- GitHubには`webapp/index.html`（更新分）と、新規ファイル`webapp/sw.js`の両方をアップロードする必要がある
- オーナー自身のスマホで、機内モード（オフライン）にした状態でもアプリが正しく開けることを確認済み

### 不具合修正：数字の高さがズレて見える（フォントのオールドスタイル数字）
「4・6」を表示すると、「6」が「4」より明らかに大きく・高く表示される不具合をオーナーのスマホで確認（ズームした画面で明確に確認できた）。原因は見出し用フォント（Georgia）の数字が、装飾的に高さの異なる「オールドスタイル数字」で描かれる仕様になっていたこと。
- 1回目の対応：`font-variant-numeric: lining-nums`を追加 → 効果なし
- 2回目の対応：`font-feature-settings: "lnum" 1, "tnum" 1;`を追加 → Georgia自体に高さの揃った代替の数字デザインが用意されていない可能性があり、CSSの指定だけでは解決できないと判断
- 最終対応：数字表示（`.result-number`・`.qr-modal-code-value`）のフォントを、見出し用のGeorgia（`--font-display`）から、本文用のシステムフォント（`--font-body`）に変更。システムフォントは標準で高さの揃った数字（ライニング数字）を使うため解消
- `app/`版はもともとシステムフォントのみを使っており、この現象自体が起きないため対応不要だった

## 追記：コード入力画面の丸い紋章を、アプリアイコンと同じ画像に変更
オーナーの要望により、`webapp/index.html`のコード入力画面（合言葉ゲート）上部にあった、CSSで描いた同心円の紋章を、アプリアイコンと同じ画像（ChatGPTで生成した三日月・星のデザイン）に差し替えた。「呼吸するような光る演出」のアニメーションはそのまま画像に適用。なお、`app/index.html`（ローカル版）にはそもそも合言葉ゲート・紋章の仕組み自体が存在しないため、対応不要だった。

### 不具合修正：サービスワーカーのキャッシュ方式により、更新内容が1回古いまま表示される
当初は「キャッシュを先に表示しつつ裏で更新する」方式（cache-first）だったため、コード修正のたびに「もう1回開き直さないと最新版が反映されない」状態になっていた（PCでは気づきにくかったが、スマホで顕在化）。「オンライン時は常に最新をネットから取得し、取得できない時だけキャッシュを使う」方式（network-first）に変更し、更新のたびに1回遅れて反映される問題を解消。あわせてキャッシュ名を`v1`→`v2`に上げ、古いキャッシュを確実に破棄するようにした。

### 不具合修正：ライフレッスンナンバーなど複数の数字を表示するカードが2行に折り返される
「4、6」のように複数の数字をまとめて表示するカードで、日本語の読点「、」の位置で改行され、数字が2行にまたがって表示されてしまう不具合を確認。`.result-number`に`white-space: nowrap;`を追加して解消（`app/style.css`・`webapp/index.html`）。あわせて、オーナーの要望により区切り文字を読点「、」から中黒丸「・」に変更（例：「4・6」）。

### 不具合修正：招待者用コードでもボタンが表示されてしまう
実装直後、招待者用コード（19810619）で入ってもボタンが表示されたままになる不具合が発生。原因は、ボタン専用に書いていたID指定のCSS（`#admin-qr-btn { display: block; }`）が、`hidden`クラス（`display: none`）よりCSSの優先度が高く、JavaScriptで`hidden`クラスを付けても表示が変わらなかったこと。`#admin-qr-btn.hidden { display: none; }`という、より優先度の高いルールを追加して解消。修正後、招待者用・管理者用の両方のコードで正しくボタンの表示が切り替わることをオーナー自身のスマホで確認済み。

## 実機（ブラウザ）での動作確認について
オーナー自身のスマートフォンで、GitHub Pages版のURL・QRコードからのアクセス、コード入力からの解錠、招待QRコード（管理者用）ボタンの表示まで、一通りの動作確認が完了した。

## 追記：計算ロジックの自動検証を追加（tests/）
「実際の生年月日・名前パターンで計算結果に違和感がないか確認する」を毎回手作業でやるのは
現実的でないため、`tests/` に自動テストを追加した。使い方は `tests/README.md` を参照。

アプリは1枚のHTMLに全部入っている作りなので、テスト用にロジックを別ファイルへコピーすると
「コピーの方だけ古いまま」が起きる。そのため**テストは毎回 `index.html` から実物のコードを
抜き出して実行する**方式にした。切り出しは行番号で行うので、`index.html` を編集すると範囲が
ずれる。ずれたまま「別のコードを検証して全部通った」となるのが最もまずいため、期待する宣言が
そろっているかを毎回確認し、ずれていればエラーで止まるようにしてある。

また、アプリのコードを写して比べても「同じ間違いをした2つを比べる」だけになるため、
`spec.md` の文章から独立に書き起こした**参照実装**を別に用意し、それと突き合わせている。

### 結果：104項目すべて合格（不具合なし）
- **ライフパスナンバー**：1900〜2025年の**全日付46,021件**を参照実装と1件ずつ照合し、全一致。
  すべて1〜9または11・22・33の範囲に収まり、範囲外の値は1件も出なかった。
  `spec.md` の例（1978年12月14日→33）も一致。
- **マスターナンバー**：46,021件の掃引の中で11・22・33のいずれも実際に出現することを確認
  （例：1970-01-29→11、1970-01-04→22、1970-05-29→33）。
  カード側も `isMaster()` で `.master` クラスが付き、CSSで金色の縁取り（`2px solid var(--gold)`＋
  グロー）が当たること、複数数字のカード（ライフレッスン等）でも同様であること、
  画面上部に「マスターナンバーが含まれています」の一文が出ることをコード上で確認した。
- **名前系の各ナンバー**：デスティニー／ソウル／パーソナリティ／ライフレッスン／
  インテンシティ／マチュリティを手計算値と照合。あわせて「母音の合計＋子音の合計＝全体の合計」が
  常に成り立つことも確認した（ソウルとパーソナリティの取りこぼしがないことの裏づけ）。
  アルファベット26文字の変換値も `spec.md` の対応表と完全一致。
- **ローマ字変換**：`spec.md` に載っている用例をすべて検証し、全一致
  （shi/cha/ji のヘボン式、hotchi・matcha の促音、taro・tokyo・toi の長音、
  keiko・heisei の「ei」、sampei・namba の ん→m、jun'ichi・hon'i・kin'yu のアポストロフィ、
  bibian・bodoreru のヴ行、カタカナの正規化、音引きの無視）。
  同じカナからは常に同じ綴りが出ること、変換済みの綴りを再変換しても変わらないことも確認。
- **アポストロフィが計算に影響しないこと**：`nameLetters()` が `[^a-z]` を除去するため、
  jun'ichi と junichi が同じ文字列になることをテストで固定した（`spec.md` の記述どおり）。

### `spec.md` の記述と実装が食い違っている箇所（実装が正しい）
長音「お段+お」について、`spec.md` は「直前の文字と同じ母音が続く場合はその2文字目を書かない
（…お段+お→o）」と例外なしで書いているが、実装は**語末の「お」だけは省略しない**。
- 実装：おおの→ono、おおにし→onishi（語中は省略）／たかとお→takatoo、せのお→senoo、
  よこお→yokoo（語末は残す）
- これは外務省のヘボン式ローマ字綴方表（パスポート）の扱いに合わせたもので、実装側の
  コメントにもその旨が書かれている。**実装の挙動の方が正しい**ため、コードは変更していない。
- ただし `spec.md` の本文（「入力項目」の長音の説明）にはこの語末の例外が書かれていないので、
  仕様書の記述を実装に合わせて補っておくと、後から読んだときの混乱を防げる。

### この自動テストで見ていないこと
検証対象は**計算とローマ字変換だけ**。画面の見た目（カードの並び、モーダルの開閉、
合言葉ゲート、QRコード表示など）は対象外なので、引き続き実機での確認が必要。

## 今後のアクション
- `index.html` の計算まわりを直したら、`cd tests && node extract.js && node test.js` を実行する
- `spec.md` の長音の説明に、語末の「お」を省略しない例外を書き足す
- 画面の見た目（マスターナンバーの金色の縁取りなど）は引き続き実機で確認する
